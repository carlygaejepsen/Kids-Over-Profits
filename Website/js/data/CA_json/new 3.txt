/* Facility Box Styling */
.facility-box {
    border: 1px solid #ccc;
    border-radius: 5px;
    margin-bottom: 1.5em;
    background-color: #fff;
}
.facility-header {
    padding: 1em;
    cursor: pointer;
    background-color: #fafafa;
}
.facility-header h1 {
    font-size: 1.5em;
    margin: 0 0 5px 0;
}
.facility-header h2 {
    font-size: 1em;
    margin: 0 0 10px 0;
    color: #555;
    font-weight: normal;
}
.inspections-container {
    padding: 0 1em 1em 1em;
    border-top: 1px solid #eee;
}
.inspection-box {
    border: 1px solid #ddd;
    margin-top: 1em;
    border-radius: 4px;
}
.inspection-header {
    padding: 0.8em;
    font-weight: bold;
    background-color: #f9f9f9;
    cursor: pointer;
}
.inspection-content {
    padding: 1em;
}
.inspection-box-violation > .inspection-header {
    background-color: #fff5f5;
    color: #c00;
}
/*End of javascript styling*/
.custom-columns-spacing p {
  margin-bottom: 5px; 
}
.center-shortcode {
  text-align: center;
  margin: 0 auto;
  width: 125px;
}
/* Main container to prevent conflicts with your theme */
.facility-report-container {
    padding: 10px;
    font-size: 16px;
}
/* Scoped Header Styles */
.facility-report-container h1 {
    font-size: 1.5em;
    font-weight: bold;
    color: #ffffff;
    margin: 0;
    padding: 2px 0;
}
.facility-report-container h2 {
    font-size: 1.1em;
    font-weight: normal;
    color: #dddddd;
    margin: 0;
    padding: 2px 0;
}
/* The main box for each facility */
.facility-report-container .facility-box {
    background-color: #00004d;
    color: #ffffff;
    padding: 20px;
    border: 1px solid #ccc;
    border-radius: 8px;
    margin-bottom: 20px;
}
.facility-report-container .facility-header,
.facility-report-container .inspection-header {
    cursor: pointer;
    padding: 10px 0;
}
.facility-report-container .facility-header .facility-details {
    font-size: 0.9em;
    font-weight: normal;
    margin: 0;
    padding: 2px 0;
}
/* Container for all inspections */
.facility-report-container .inspections-container {
    padding-top: 15px;
    border-top: 2px solid #ddd;
    margin-top: 15px;
}
/* The box for a single inspection */
.facility-report-container .inspection-box {
    padding: 10px 15px;
    border: 1px solid #ddd;
    margin-top: 10px;
    border-radius: 5px;
}
.facility-report-container .inspection-box-violation {
    background-color: #ff9933;
    color: #ffffff;
}
.facility-report-container .inspection-box-clean {
    background-color: #ffffff;
    color: #000000;
}
.facility-report-container .inspection-content {
    padding-top: 10px;
    border-top: 1px solid #eee;
    margin-top: 10px;
    font-size: 0.9em;
}
/* Fix the show/hide indicators */
.facility-report-container .indicator .open-text { display: none !important; }
.facility-report-container .indicator .closed-text { display: inline !important; }
.facility-report-container details[open] > summary .indicator .open-text { display: inline !important; }
.facility-report-container details[open] > summary .indicator .closed-text { display: none !important; }
/* Make sure the indicator-compact class uses the same logic */
.facility-report-container .indicator-compact .open-text { display: none !important; }
.facility-report-container .indicator-compact .closed-text { display: inline !important; }
.facility-report-container details[open] > summary .indicator-compact .open-text { display: inline !important; }
.facility-report-container details[open] > summary .indicator-compact .closed-text { display: none !important; }
/* New block for the non-collapsible inspection details */
.facility-report-container .inspection-details-block {
    background-color: #f7f7f7;
    color: #000;
    padding: 10px;
    border-radius: 4px;
    margin-bottom: 15px;
}
.facility-report-container .inspection-details-block p {
    margin: 5px 0 0 0;
}
/* The box for a single violation (collapsible) */
.facility-report-container .violation-box {
    margin-left: 20px;
    margin-bottom: 10px;
    background-color: #ffffff;
    color: #000;
    padding: 5px 10px;
    border: 1px solid #ddd;
    border-radius: 4px;
}
.facility-report-container .deficiency-header {
    font-weight: bold;
    cursor: pointer;
    display: flex;
    justify-content: space-between;
    align-items: center;
}
/* The three nested boxes for Rule, Evidence, Findings */
.facility-report-container .deficiency-box {
    margin-top: 5px;
    background-color: #f7f7f7;
    padding: 5px 10px;
    border: 1px solid #e0e0e0;
    border-radius: 4px;
}
.facility-report-container .deficiency-content {
    padding: 10px;
    border-top: 1px solid #eee;
    margin-top: 5px;
}
/* TOC Styles */
.toc_title, 
.ez-toc-title,
.lwptoc_header {
    color: #000000 !important; 
}
.widget_toc, 
.widget[id*="toc"], 
.widget[class*="toc"] {
    background-color: #ffffff;
    padding: 15px;
    border-radius: 5px;
}
.toc_title,
.ez-toc-title,
.lwptoc_header {
    margin: 0 !important;
    padding: 0.25em 0.5em !important;
    font-size: 1em !important;
    line-height: 1.2 !important;
}
.widget_toc ul,
.widget[id*="toc"] ul,
.widget[class*="toc"] ul {
    margin: 0 !important;
    padding-left: 1em !important;
}
.widget_toc li,
.widget[id*="toc"] li,
.widget[class*="toc"] li {
    margin: 0 !important;
    padding: 0.1em 0 !important;
    line-height: 1.3 !important;
}
.widget_toc ol,
.widget[id*="toc"] ol,
.widget[class*="toc"] ol {
    list-style: none !important;
    padding-left: 0 !important;
    margin-left: 0 !important;
}
.sidebar .ez_toc_widget-container:not(.ez-toc-collapsed),
.ez-toc:not(.ez-toc-collapsed) {
    max-height: 200px;
    overflow-y: auto;
}
.sidebar .ez_toc_widget-container.ez-toc-collapsed,
.ez-toc.ez-toc-collapsed {
    max-height: none;
    height: auto;
    overflow: hidden;
}
.ez-toc-toggle {
  background-color: #fff !important;
  background-clip: padding-box !important;
  background-image: none !important;
}
/* Hides the default arrow */
.facility-report-container summary::-webkit-details-marker {
    display: none;
}
.facility-report-container summary {
    list-style-type: none;
    cursor: pointer;
}
/* The +/- indicators */
.facility-report-container .indicator .open-text { display: none; }
.facility-report-container .indicator .closed-text { display: inline; }
.facility-report-container details[open] > summary .indicator .open-text { display: inline; }
.facility-report-container details[open] > summary .indicator .closed-text { display: none; }
.facility-report-container .indicator-main {
    font-size: 0.8em;
    margin-top: 10px;
    font-weight: normal;
}
.facility-report-container .indicator-inspection {
    font-size: 0.9em;
    font-weight: normal;
    margin-left: auto;
    padding-left: 15px;
}
.facility-report-container [id]::before {
  content: "";
  display: block;
  height: 80px;
  margin-top: -80px;
  visibility: hidden;
}
/* FACILITY DIRECTORY ADDITIONS */
.operators-grid::after {
    content: "";
    display: table;
    clear: both;
}
.operator-header-compact { 
    padding: 0 !important;
    display: block !important;
    height: 100% !important; /* Fill the available height */
    position: relative !important;
}
.operator-narrow { 
    float: left !important;
    width: 220px !important; 
    height: 200px !important;
    margin: 0 15px 15px 0 !important;
    background-color: #00004d !important; 
    color: #ffffff !important; 
    padding: 12px !important; 
    border-radius: 6px !important; 
    box-sizing: border-box !important;
    position: relative !important;
}
/* When expanded, stay in place but appear on top */
.operator-narrow details[open] {
    position: relative !important;
    z-index: 999 !important;
    background-color: #00004d !important;
    border-radius: 6px !important;
    box-shadow: 0 4px 8px rgba(0,0,0,0.3) !important;
    /* Remove the top/left positioning that was causing the jump */
}
.operator-name-small h1 { 
    color: #ffffff !important; 
    margin: 0 0 4px 0 !important; 
    font-size: 1.0em !important; 
    font-weight: bold !important; 
    line-height: 1.1 !important;
    word-wrap: break-word !important;
    /* Remove all the truncation stuff */
}
.indicator-compact { 
    font-size: 0.75em !important; 
    color: #cccccc !important; 
}
.facilities-list { 
    padding: 12px !important; 
    border-top: 1px solid #555 !important; 
    margin-top: 8px !important; 
    background-color: rgba(255,255,255,0.1) !important;
    border-radius: 4px !important;
}
.facility-card { 
    padding: 8px !important; 
    border-radius: 4px !important; 
    margin-bottom: 6px !important; 
    background-color: #ffffff !important;
    color: #000000 !important;
    word-wrap: break-word !important;
}
.facility-name-header h2 { 
    font-size: 0.9em !important; 
    font-weight: bold !important; 
    margin: 0 0 4px 0 !important; 
    color: #000000 !important; 
    line-height: 1.2 !important;
}
.facility-details-small { 
    font-size: 0.75em !important; 
    color: #666666 !important; 
    font-weight: normal !important; 
    line-height: 1.3 !important;
}
/* Status colors */
/* Status colors - border instead of full background */
.facility-card {
    padding: 8px !important; 
    border-radius: 4px !important; 
    margin-bottom: 6px !important; 
    background-color: #ffffff !important;
    color: #000000 !important;
    word-wrap: break-word !important;
    border: 5px solid #fff !important; /* Default light border */
}
.facility-card.status-open { 
    border-color: #ef9034 !important; /* Corrected orange */
}
.facility-card.status-transferred { 
    border-color: #888888 !important; 
}
.facility-card.status-closed { 
    border-color: #33a7b5 !important; 
}
/* Keep text black on white background for all cards */
.facility-card.status-open h2,
.facility-card.status-open .facility-details-small,
.facility-card.status-open .facility-other-names {
    color: #000000 !important; 
}
.facility-card.status-transferred h2,
.facility-card.status-transferred .facility-details-small,
.facility-card.status-transferred .facility-other-names {
    color: #000000 !important; 
}
.facility-card.status-closed h2,
.facility-card.status-closed .facility-details-small,
.facility-card.status-closed .facility-other-names {
    color: #000000 !important; 
}
/* Website links can stay blue since background is white */
.facility-website a {
    color: #0066cc !important;
    border-color: #0066cc !important;
}
.facility-website a:hover {
    background-color: #0066cc !important;
    color: white !important;
}
/* Control buttons at the top */
.control-buttons {
    margin-bottom: 20px;
    text-align: center;
}
.expand-all-btn,
.collapse-all-btn {
    background-color: #00004d;
    color: white;
    border: none;
    padding: 10px 20px;
    margin: 0 5px;
    border-radius: 5px;
    cursor: pointer;
    font-size: 14px;
}
.expand-all-btn:hover,
.collapse-all-btn:hover {
    background-color: #000066;
}
/* Close button at bottom of expanded boxes */
.close-button-container {
    text-align: center;
    padding: 10px;
    border-top: 1px solid #555;
    margin-top: 10px;
}
.close-btn {
    background-color: #666;
    color: white;
    border: none;
    padding: 8px 16px;
    border-radius: 4px;
    cursor: pointer;
    font-size: 12px;
}
.close-btn:hover {
    background-color: #888;
}