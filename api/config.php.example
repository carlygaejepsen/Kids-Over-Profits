<?php
/**
 * Database configuration - EXAMPLE
 *
 * DO NOT COMMIT THE ACTUAL CONFIG.PHP FILE WITH YOUR PASSWORD.
 *
 * To use, copy this file to `config.php` and fill in your actual credentials.
 * The `config.php` file is ignored by Git to protect your secrets.
 */

/**
 * Option 1: expose credentials via WordPress constants or environment variables
 * -----------------------------------------------------------------------------
 * Define KOP_DB_* (or DB_*) constants in wp-config.php or export matching
 * environment variables. The runtime loader in api/config.php will discover
 * them automatically.
 */

/**
 * Option 2: create config.local.php for standalone usage
 * ------------------------------------------------------
 * If you need these scripts to run without WordPress bootstrapping (e.g. when
 * executing them directly from the command line), copy this file to
 * `config.local.php` and update the values below. The loader prefers
 * constants/environment variables, but will fall back to the entries returned
 * from this file when present.
 */

return [
    'host' => 'localhost',
    'name' => 'kidsover_suggestions',
    'user' => 'kidsover_dani',
    'pass' => 'YOUR_DATABASE_PASSWORD_HERE',
];
